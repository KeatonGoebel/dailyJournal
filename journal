create database journal;

create Table
    userData (
        email VARCHAR(100),
        currentDay DATE,
        mood VARCHAR(1000),
        rating INT (4),
        favorite_thing VARCHAR(1000),
        PRIMARY KEY (email, currentDay)
    );
